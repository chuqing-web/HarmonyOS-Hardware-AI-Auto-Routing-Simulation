{
  "line": 597,
  "name": "StrReplace",
  "path": "c:\\Projects\\ElecDraw_Harmony\\tools\\pcb_templates\\_hand_analog_ic.mjs",
  "hit": [],
  "old_string": "  // LM_OUT↑y=100；LM_FB 先向下再走 y=520，避开扇出高度\n  {\n    const a = pw('U3', 1); const b = pw('Rf3', 2);\n    if (a && b) {\n      const ea = { x: a.x - 55, y: a.y }; const eb = { x: b.x, y: b.y - 40 };\n      add(lmOut, a, ea, 12, 'F.Cu'); addVia(lmOut, ea);\n      L(lmOut, [ea, { x: ea.x, y: 100 }, { x: eb.x, y: 100 }, eb], 12, 'B.Cu');\n      addVia(lmOut, eb); add(lmOut, eb, b, 12, 'F.Cu');\n    }\n  }\n  {\n    const a = pw('U3', 2); const b = pw('Rf3', 1);\n    if (a && b) {\n      const ea = { x: a.x - 90, y: a.y + 70 }; const eb = { x: b.x, y: b.y + 70 };\n      add(lmFb, a, ea, 12, 'F.Cu'); addVia(lmFb, ea);\n      L(lmFb, [ea, { x: ea.x, y: 520 }, { x: eb.x, y: 520 }, eb], 12, 'B.Cu');\n      addVia(lmFb, eb); add(lmFb, eb, b, 12, 'F.Cu');\n    }\n  }\n  {\n    const a = pw('Rf3', 1); const b = pw('Rg3', 1);\n    if (a && b) {\n      const ea = { x: a.x, y: a.y + 70 }; const eb = { x: b.x, y: b.y - 40 };\n      add(lmFb, a, ea, 12, 'F.Cu'); addVia(lmFb, ea);\n      L(lmFb, [ea, { x: ea.x, y: 520 }, { x: eb.x, y: 520 }, eb], 12, 'B.Cu');\n      addVia(lmFb, eb); add(lmFb, eb, b, 12, 'F.Cu');\n    }\n  }\n  add(u3b, pw('U3', 7), pw('U3', 6), 10, 'F.Cu');\n\n  // —— 555：DISCH y=400；CAP y=620；TRIG-THRES y=720（均低于扇出）——\n  add(disch, pw('RA', 2), pw('RB', 1), 12, 'F.Cu');\n  {\n    const a = pw('RB', 1); const b = pw('U5', 7);\n    if (a && b) {\n      const ea = { x: a.x + 55, y: a.y + 50 }; const eb = { x: b.x - 70, y: b.y + 50 };\n      add(disch, a, ea, 12, 'F.Cu'); addVia(disch, ea);\n      L(disch, [ea, { x: ea.x, y: 400 }, { x: eb.x, y: 400 }, eb], 12, 'B.Cu');\n      addVia(disch, eb); add(disch, eb, b, 12, 'F.Cu');\n    }\n  }\n  add(cap555, pw('RB', 2), pw('CT', 1), 12, 'F.Cu');\n  {\n    const a = pw('CT', 1); const b = pw('U5', 2);\n    if (a && b) {\n      const ea = { x: a.x - 55, y: a.y + 40 }; const eb = { x: b.x - 70, y: b.y + 40 };\n      add(cap555, a, ea, 12, 'F.Cu'); addVia(cap555, ea);\n      L(cap555, [ea, { x: ea.x, y: 620 }, { x: eb.x, y: 620 }, eb], 12, 'B.Cu');\n      addVia(cap555, eb); add(cap555, eb, b, 12, 'F.Cu');\n    }\n  }\n  {\n    const t2 = pw('U5', 2);\n    const t6 = pw('U5', 6);\n    if (t2 && t6) {\n      const e2 = { x: t2.x - 50, y: t2.y + 40 };\n      const e6 = { x: t6.x + 50, y: t6.y + 40 };\n      add(cap555, t2, e2, 12, 'F.Cu');\n      addVia(cap555, e2);\n      L(cap555, [e2, { x: e2.x, y: 720 }, { x: e6.x, y: 720 }, e6], 12, 'B.Cu');\n      addVia(cap555, e6);\n      add(cap555, e6, t6, 12, 'F.Cu');\n    }\n  }\n  add(out555, pw('U5', 3), pw('RLED', 1), 12, 'F.Cu');\n  add(led555, pw('RLED', 2), pw('D555', 1), 12, 'F.Cu');\n  add(ctrl, pw('U5', 5), pw('CC555', 1), 12, 'F.Cu');\n\n  // —— 稳压 / Buck ——\n  for (let i = 1; i <= 3; i++) {\n    const vout = i === 1 ? vout0 : i === 2 ? vout1 : vout2;\n    add(vout, pw(`REG${i}`, 3), pw(`CO${i}`, 1), 14, 'F.Cu');\n    add(vout, pw(`CO${i}`, 1), pw(`RL${i}`, 1), 12, 'F.Cu');\n  }\n  {\n    const a = pw('U4', 2); const b = pw('LB', 1);\n    if (a && b) {\n      L(buckSw, [a, { x: a.x, y: a.y - 80 }, { x: b.x, y: b.y - 80 }, b], 14, 'F.Cu');\n    }\n  }\n  add(buckOut, pw('LB', 2), pw('CBO', 1), 14, 'F.Cu');\n  routeB(buckOut, pw('CBO', 1), pw('U4', 4), 12, -45, 0, 0, 50);\n  add(buckOut, pw('U4', 4), pw('U4', 5), 12, 'F.Cu');\n  add(buckOut, pw('CBO', 1), pw('RFB', 1), 12, 'F.Cu');\n\n  // —— 排针：F stub → B 先水平进右缘列再升降（避开板内本地 B 竖段）——\n  const jGnd = pw('J1', 1);\n  const jVcc = pw('J1', 2);\n  const jSig = pw('J1', 3);\n  const jLm = pw('J1', 4);\n  const j555 = pw('J1', 5);\n  const jV0 = pw('J1', 6);\n  const jBk = pw('J1', 7);\n  const jDs = pw('J1', 8);\n  if (gnd && jGnd) addVia(gnd, jGnd);\n  if (vcc && jVcc) addVia(vcc, jVcc);\n\n  let slot = 0;\n  const toHdr = (net, src, jPad) => {\n    if (!net || !src || !jPad) return;\n    const s = slot++;\n    const colX = 2500 + s * 42;\n    const riseX = 2700 + s * 40;\n    const runY = 45 + s * 34;\n    const esc = { x: src.x + (src.x < 2400 ? 45 : -45), y: src.y };\n    add(net, src, esc, 12, 'F.Cu');\n    addVia(net, esc);\n    L(net, [\n      esc,\n      { x: colX, y: esc.y },\n      { x: colX, y: runY },\n      { x: riseX, y: runY },\n      { x: riseX, y: jPad.y }\n    ], 12, 'B.Cu');\n    addVia(net, { x: riseX, y: jPad.y });\n    add(net, { x: riseX, y: jPad.y }, jPad, 12, 'F.Cu');\n  };\n  toHdr(sig, pw('R1', 1), jSig);\n  toHdr(lmOut, pw('Rf3', 2), jLm);\n  toHdr(out555, pw('RLED', 1), j555);\n  toHdr(disch, pw('RA', 2), jDs);\n  toHdr(vout0, pw('REG1', 3), jV0);\n  toHdr(buckOut, pw('CBO', 1), jBk);",
  "new_string": "  // LM_OUT↑y=100；LM_FB 先向下再走 y=520，避开 LM_IN\n  {\n    const a = pw('U3', 1); const b = pw('Rf3', 2);\n    if (a && b) {\n      const ea = { x: a.x - 55, y: a.y }; const eb = { x: b.x, y: b.y - 40 };\n      add(lmOut, a, ea, 12, 'F.Cu'); addVia(lmOut, ea);\n      L(lmOut, [ea, { x: ea.x, y: 100 }, { x: eb.x, y: 100 }, eb], 12, 'B.Cu');\n      addVia(lmOut, eb); add(lmOut, eb, b, 12, 'F.Cu');\n    }\n  }\n  {\n    const a = pw('U3', 2); const b = pw('Rf3', 1);\n    if (a && b) {\n      const mid = { x: a.x, y: a.y + 90 };\n      const ea = { x: a.x - 90, y: a.y + 90 };\n      const eb = { x: b.x, y: b.y + 70 };\n      add(lmFb, a, mid, 12, 'F.Cu'); add(lmFb, mid, ea, 12, 'F.Cu'); addVia(lmFb, ea);\n      L(lmFb, [ea, { x: ea.x, y: 520 }, { x: eb.x, y: 520 }, eb], 12, 'B.Cu');\n      addVia(lmFb, eb); add(lmFb, eb, b, 12, 'F.Cu');\n    }\n  }\n  {\n    const a = pw('Rf3', 1); const b = pw('Rg3', 1);\n    if (a && b) {\n      const ea = { x: a.x, y: a.y + 70 }; const eb = { x: b.x, y: b.y - 40 };\n      add(lmFb, a, ea, 12, 'F.Cu'); addVia(lmFb, ea);\n      L(lmFb, [ea, { x: ea.x, y: 520 }, { x: eb.x, y: 520 }, eb], 12, 'B.Cu');\n      addVia(lmFb, eb); add(lmFb, eb, b, 12, 'F.Cu');\n    }\n  }\n  add(u3b, pw('U3', 7), pw('U3', 6), 10, 'F.Cu');\n\n  // —— 555 ——\n  add(disch, pw('RA', 2), pw('RB', 1), 12, 'F.Cu');\n  {\n    const a = pw('RB', 1); const b = pw('U5', 7);\n    if (a && b) {\n      const ea = { x: a.x, y: a.y + 55 }; const eb = { x: b.x, y: b.y + 55 };\n      add(disch, a, ea, 12, 'F.Cu'); addVia(disch, ea);\n      L(disch, [ea, { x: ea.x, y: 400 }, { x: eb.x, y: 400 }, eb], 12, 'B.Cu');\n      addVia(disch, eb); add(disch, eb, b, 12, 'F.Cu');\n    }\n  }\n  add(cap555, pw('RB', 2), pw('CT', 1), 12, 'F.Cu');\n  {\n    const a = pw('CT', 1); const b = pw('U5', 2);\n    if (a && b) {\n      const ea = { x: a.x, y: a.y + 55 }; const eb = { x: b.x - 80, y: b.y };\n      add(cap555, a, ea, 12, 'F.Cu'); addVia(cap555, ea);\n      L(cap555, [ea, { x: ea.x, y: 620 }, { x: eb.x, y: 620 }, eb], 12, 'B.Cu');\n      addVia(cap555, eb); add(cap555, eb, b, 12, 'F.Cu');\n    }\n  }\n  {\n    const t2 = pw('U5', 2);\n    const t6 = pw('U5', 6);\n    if (t2 && t6) {\n      const e2 = { x: t2.x - 80, y: t2.y };\n      const e6 = { x: t6.x + 80, y: t6.y };\n      add(cap555, t2, e2, 12, 'F.Cu');\n      addVia(cap555, e2);\n      L(cap555, [e2, { x: e2.x, y: 720 }, { x: e6.x, y: 720 }, e6], 12, 'B.Cu');\n      addVia(cap555, e6);\n      add(cap555, e6, t6, 12, 'F.Cu');\n    }\n  }\n  // OUT 上绕，避开 DISCH 回焊盘 stub\n  {\n    const a = pw('U5', 3); const b = pw('RLED', 1);\n    if (a && b) {\n      L(out555, [a, { x: a.x, y: a.y - 60 }, { x: b.x, y: b.y - 60 }, b], 12, 'F.Cu');\n    }\n  }\n  add(led555, pw('RLED', 2), pw('D555', 1), 12, 'F.Cu');\n  add(ctrl, pw('U5', 5), pw('CC555', 1), 12, 'F.Cu');\n\n  // —— 稳压 / Buck ——\n  for (let i = 1; i <= 3; i++) {\n    const vout = i === 1 ? vout0 : i === 2 ? vout1 : vout2;\n    add(vout, pw(`REG${i}`, 3), pw(`CO${i}`, 1), 14, 'F.Cu');\n    add(vout, pw(`CO${i}`, 1), pw(`RL${i}`, 1), 12, 'F.Cu');\n  }\n  {\n    const a = pw('U4', 2); const b = pw('LB', 1);\n    if (a && b) {\n      L(buckSw, [a, { x: a.x, y: a.y - 80 }, { x: b.x, y: b.y - 80 }, b], 14, 'F.Cu');\n    }\n  }\n  add(buckOut, pw('LB', 2), pw('CBO', 1), 14, 'F.Cu');\n  routeB(buckOut, pw('CBO', 1), pw('U4', 4), 12, -45, 0, 0, 50);\n  add(buckOut, pw('U4', 4), pw('U4', 5), 12, 'F.Cu');\n  add(buckOut, pw('CBO', 1), pw('RFB', 1), 12, 'F.Cu');\n\n  // —— 排针：按源 X 排序，V-first + 互异 runY/colX（HV 无交叉）——\n  const jGnd = pw('J1', 1);\n  const jVcc = pw('J1', 2);\n  const jSig = pw('J1', 3);\n  const jLm = pw('J1', 4);\n  const j555 = pw('J1', 5);\n  const jV0 = pw('J1', 6);\n  const jBk = pw('J1', 7);\n  const jDs = pw('J1', 8);\n  if (gnd && jGnd) addVia(gnd, jGnd);\n  if (vcc && jVcc) addVia(vcc, jVcc);\n\n  const fanJobs = [\n    { net: sig, src: pw('R1', 1), jPad: jSig },\n    { net: vout0, src: pw('REG1', 3), jPad: jV0 },\n    { net: lmOut, src: pw('Rf3', 2), jPad: jLm },\n    { net: disch, src: pw('RA', 2), jPad: jDs },\n    { net: out555, src: pw('RLED', 1), jPad: j555 },\n    { net: buckOut, src: pw('CBO', 1), jPad: jBk }\n  ].filter(j => j.net && j.src && j.jPad)\n    .sort((a, b) => a.src.x - b.src.x || a.src.y - b.src.y);\n\n  fanJobs.forEach((job, s) => {\n    const colX = 2520 + s * 48;\n    const runY = 40 + s * 36;\n    const esc = { x: job.src.x + (job.src.x < 2400 ? 40 : -40), y: job.src.y };\n    add(job.net, job.src, esc, 12, 'F.Cu');\n    addVia(job.net, esc);\n    L(job.net, [\n      esc,\n      { x: esc.x, y: runY },\n      { x: colX, y: runY },\n      { x: colX, y: job.jPad.y }\n    ], 12, 'B.Cu');\n    addVia(job.net, { x: colX, y: job.jPad.y });\n    add(job.net, { x: colX, y: job.jPad.y }, job.jPad, 12, 'F.Cu');\n  });"
}