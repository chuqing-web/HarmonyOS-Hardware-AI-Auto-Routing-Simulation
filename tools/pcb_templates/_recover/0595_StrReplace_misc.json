{
  "line": 595,
  "name": "StrReplace",
  "path": "c:\\Projects\\ElecDraw_Harmony\\tools\\pcb_templates\\_hand_analog_ic.mjs",
  "hit": [],
  "old_string": "  // LM_OUT 右下逃；LM_FB 左下逃 — B 廊错开\n  routeB(lmOut, pw('U3', 1), pw('Rf3', 2), 12, -50, 0, 0, 50, true);\n  routeB(lmFb, pw('U3', 2), pw('Rf3', 1), 12, -80, 0, 0, -50, false);\n  routeB(lmFb, pw('Rf3', 1), pw('Rg3', 1), 12, -50, 0, 0, -50, false);\n  add(u3b, pw('U3', 7), pw('U3', 6), 10, 'F.Cu');\n\n  // —— 555 ——\n  add(disch, pw('RA', 2), pw('RB', 1), 12, 'F.Cu');\n  routeB(disch, pw('RB', 1), pw('U5', 7), 12, 50, 0, -60, 0, true);\n  add(cap555, pw('RB', 2), pw('CT', 1), 12, 'F.Cu');\n  routeB(cap555, pw('CT', 1), pw('U5', 2), 12, -50, 0, -60, 0, false);\n  {\n    const t2 = pw('U5', 2);\n    const t6 = pw('U5', 6);\n    if (t2 && t6) {\n      const e2 = { x: t2.x - 40, y: t2.y };\n      const e6 = { x: t6.x + 40, y: t6.y };\n      add(cap555, t2, e2, 12, 'F.Cu');\n      addVia(cap555, e2);\n      L(cap555, [e2, { x: e2.x, y: 180 }, { x: e6.x, y: 180 }, e6], 12, 'B.Cu');\n      addVia(cap555, e6);\n      add(cap555, e6, t6, 12, 'F.Cu');\n    }\n  }\n  add(out555, pw('U5', 3), pw('RLED', 1), 12, 'F.Cu');\n  add(led555, pw('RLED', 2), pw('D555', 1), 12, 'F.Cu');\n  add(ctrl, pw('U5', 5), pw('CC555', 1), 12, 'F.Cu');\n\n  // —— 稳压 / Buck：本地 F ——\n  for (let i = 1; i <= 3; i++) {\n    const vout = i === 1 ? vout0 : i === 2 ? vout1 : vout2;\n    add(vout, pw(`REG${i}`, 3), pw(`CO${i}`, 1), 14, 'F.Cu');\n    add(vout, pw(`CO${i}`, 1), pw(`RL${i}`, 1), 12, 'F.Cu');\n  }\n  add(buckSw, pw('U4', 2), pw('LB', 1), 14, 'F.Cu');\n  add(buckOut, pw('LB', 2), pw('CBO', 1), 14, 'F.Cu');\n  routeB(buckOut, pw('CBO', 1), pw('U4', 4), 12, -45, 0, 0, 50);\n  add(buckOut, pw('U4', 4), pw('U4', 5), 12, 'F.Cu');\n  add(buckOut, pw('CBO', 1), pw('RFB', 1), 12, 'F.Cu');\n\n  // —— 排针：横 F / 竖 B，互异 riseX·runY ——\n  const jGnd = pw('J1', 1);\n  const jVcc = pw('J1', 2);\n  const jSig = pw('J1', 3);\n  const jLm = pw('J1', 4);\n  const j555 = pw('J1', 5);\n  const jV0 = pw('J1', 6);\n  const jBk = pw('J1', 7);\n  const jDs = pw('J1', 8);\n  if (gnd && jGnd) addVia(gnd, jGnd);\n  if (vcc && jVcc) addVia(vcc, jVcc);\n\n  let slot = 0;\n  const toHdr = (net, src, jPad) => {\n    if (!net || !src || !jPad) return;\n    const s = slot++;\n    const riseX = 2650 + s * 45;\n    const runY = 60 + s * 32;\n    // 板内走 F（横/竖分层靠互异 runY）；仅右缘 riseX 用 B 下落，避开本地 B 绕行\n    L(net, [\n      src, { x: src.x, y: runY }, { x: riseX, y: runY }\n    ], 12, 'F.Cu');\n    addVia(net, { x: riseX, y: runY });\n    add(net, { x: riseX, y: runY }, { x: riseX, y: jPad.y }, 12, 'B.Cu');\n    addVia(net, { x: riseX, y: jPad.y });\n    add(net, { x: riseX, y: jPad.y }, jPad, 12, 'F.Cu');\n  };\n  toHdr(sig, pw('R1', 1), jSig);\n  toHdr(lmOut, pw('Rf3', 2), jLm);\n  toHdr(out555, pw('RLED', 2), j555);\n  toHdr(disch, pw('RA', 2), jDs);\n  toHdr(vout0, pw('REG1', 3), jV0);\n  toHdr(buckOut, pw('CBO', 1), jBk);",
  "new_string": "  // LM_OUT / LM_FB：独立 B 廊（OUT 走 y=120，FB 走 y=520）\n  {\n    const a = pw('U3', 1); const b = pw('Rf3', 2);\n    if (a && b) {\n      const ea = { x: a.x - 55, y: a.y }; const eb = { x: b.x, y: b.y + 55 };\n      add(lmOut, a, ea, 12, 'F.Cu'); addVia(lmOut, ea);\n      L(lmOut, [ea, { x: ea.x, y: 120 }, { x: eb.x, y: 120 }, eb], 12, 'B.Cu');\n      addVia(lmOut, eb); add(lmOut, eb, b, 12, 'F.Cu');\n    }\n  }\n  {\n    const a = pw('U3', 2); const b = pw('Rf3', 1);\n    if (a && b) {\n      const ea = { x: a.x - 90, y: a.y }; const eb = { x: b.x, y: b.y - 55 };\n      add(lmFb, a, ea, 12, 'F.Cu'); addVia(lmFb, ea);\n      L(lmFb, [ea, { x: ea.x, y: 520 }, { x: eb.x, y: 520 }, eb], 12, 'B.Cu');\n      addVia(lmFb, eb); add(lmFb, eb, b, 12, 'F.Cu');\n    }\n  }\n  {\n    const a = pw('Rf3', 1); const b = pw('Rg3', 1);\n    if (a && b) {\n      const ea = { x: a.x - 55, y: a.y }; const eb = { x: b.x, y: b.y - 55 };\n      add(lmFb, a, ea, 12, 'F.Cu'); addVia(lmFb, ea);\n      L(lmFb, [ea, { x: ea.x, y: 520 }, { x: eb.x, y: 520 }, eb], 12, 'B.Cu');\n      addVia(lmFb, eb); add(lmFb, eb, b, 12, 'F.Cu');\n    }\n  }\n  add(u3b, pw('U3', 7), pw('U3', 6), 10, 'F.Cu');\n\n  // —— 555：DISCH y=360；CAP y=600；TRIG-THRES y=700 ——\n  add(disch, pw('RA', 2), pw('RB', 1), 12, 'F.Cu');\n  {\n    const a = pw('RB', 1); const b = pw('U5', 7);\n    if (a && b) {\n      const ea = { x: a.x + 55, y: a.y }; const eb = { x: b.x - 70, y: b.y };\n      add(disch, a, ea, 12, 'F.Cu'); addVia(disch, ea);\n      L(disch, [ea, { x: ea.x, y: 360 }, { x: eb.x, y: 360 }, eb], 12, 'B.Cu');\n      addVia(disch, eb); add(disch, eb, b, 12, 'F.Cu');\n    }\n  }\n  add(cap555, pw('RB', 2), pw('CT', 1), 12, 'F.Cu');\n  {\n    const a = pw('CT', 1); const b = pw('U5', 2);\n    if (a && b) {\n      const ea = { x: a.x - 55, y: a.y }; const eb = { x: b.x - 70, y: b.y };\n      add(cap555, a, ea, 12, 'F.Cu'); addVia(cap555, ea);\n      L(cap555, [ea, { x: ea.x, y: 600 }, { x: eb.x, y: 600 }, eb], 12, 'B.Cu');\n      addVia(cap555, eb); add(cap555, eb, b, 12, 'F.Cu');\n    }\n  }\n  {\n    const t2 = pw('U5', 2);\n    const t6 = pw('U5', 6);\n    if (t2 && t6) {\n      const e2 = { x: t2.x - 50, y: t2.y };\n      const e6 = { x: t6.x + 50, y: t6.y };\n      add(cap555, t2, e2, 12, 'F.Cu');\n      addVia(cap555, e2);\n      L(cap555, [e2, { x: e2.x, y: 700 }, { x: e6.x, y: 700 }, e6], 12, 'B.Cu');\n      addVia(cap555, e6);\n      add(cap555, e6, t6, 12, 'F.Cu');\n    }\n  }\n  add(out555, pw('U5', 3), pw('RLED', 1), 12, 'F.Cu');\n  add(led555, pw('RLED', 2), pw('D555', 1), 12, 'F.Cu');\n  add(ctrl, pw('U5', 5), pw('CC555', 1), 12, 'F.Cu');\n\n  // —— 稳压 / Buck ——\n  for (let i = 1; i <= 3; i++) {\n    const vout = i === 1 ? vout0 : i === 2 ? vout1 : vout2;\n    add(vout, pw(`REG${i}`, 3), pw(`CO${i}`, 1), 14, 'F.Cu');\n    add(vout, pw(`CO${i}`, 1), pw(`RL${i}`, 1), 12, 'F.Cu');\n  }\n  // SW / OUT 错开：SW 上绕 F，OUT 本地短连\n  {\n    const a = pw('U4', 2); const b = pw('LB', 1);\n    if (a && b) {\n      L(buckSw, [a, { x: a.x, y: a.y - 80 }, { x: b.x, y: b.y - 80 }, b], 14, 'F.Cu');\n    }\n  }\n  add(buckOut, pw('LB', 2), pw('CBO', 1), 14, 'F.Cu');\n  routeB(buckOut, pw('CBO', 1), pw('U4', 4), 12, -45, 0, 0, 50);\n  add(buckOut, pw('U4', 4), pw('U4', 5), 12, 'F.Cu');\n  add(buckOut, pw('CBO', 1), pw('RFB', 1), 12, 'F.Cu');\n\n  // —— 排针：短 F stub → 右缘走廊 B（x≥2480），避开板内 B 绕行 ——\n  const jGnd = pw('J1', 1);\n  const jVcc = pw('J1', 2);\n  const jSig = pw('J1', 3);\n  const jLm = pw('J1', 4);\n  const j555 = pw('J1', 5);\n  const jV0 = pw('J1', 6);\n  const jBk = pw('J1', 7);\n  const jDs = pw('J1', 8);\n  if (gnd && jGnd) addVia(gnd, jGnd);\n  if (vcc && jVcc) addVia(vcc, jVcc);\n\n  let slot = 0;\n  const toHdr = (net, src, jPad) => {\n    if (!net || !src || !jPad) return;\n    const s = slot++;\n    const colX = 2480 + s * 40;\n    const riseX = 2680 + s * 40;\n    const runY = 50 + s * 36;\n    const esc = { x: src.x + (src.x < 2400 ? 40 : -40), y: src.y };\n    add(net, src, esc, 12, 'F.Cu');\n    addVia(net, esc);\n    L(net, [\n      esc,\n      { x: colX, y: esc.y },\n      { x: colX, y: runY },\n      { x: riseX, y: runY },\n      { x: riseX, y: jPad.y }\n    ], 12, 'B.Cu');\n    addVia(net, { x: riseX, y: jPad.y });\n    add(net, { x: riseX, y: jPad.y }, jPad, 12, 'F.Cu');\n  };\n  toHdr(sig, pw('R1', 1), jSig);\n  toHdr(lmOut, pw('Rf3', 2), jLm);\n  toHdr(out555, pw('RLED', 1), j555);\n  toHdr(disch, pw('RA', 2), jDs);\n  toHdr(vout0, pw('REG1', 3), jV0);\n  toHdr(buckOut, pw('CBO', 1), jBk);"
}