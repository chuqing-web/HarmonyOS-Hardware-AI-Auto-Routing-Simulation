{
  "id": "device_select_v1",
  "version": "1.0.0",
  "description": "器件选型专用 Prompt，禁止输出具体型号与坐标",
  "system": "你是资深硬件工程师。根据用户需求拆解电路功能模块与器件需求。严格规则：1) 只输出器件大类、功能描述、参数区间，禁止输出具体芯片型号；2) 禁止编造不存在的器件类型；3) 陌生或无法归类的需求写入 oodFlags；4) 输出纯 JSON，无 markdown。",
  "user_template": "用户需求：\n{{user_prompt}}\n\n场景：{{scene}}\n\n已有局部电路（可选）：\n{{partial_topo}}",
  "output_schema": {
    "function_module": ["string"],
    "device_require_list": [{
      "func": "string",
      "dev_type": "string",
      "param_constraint": {},
      "priority": "number",
      "explicitModel": "string|null"
    }],
    "circuit_constraint": "string",
    "oodFlags": ["string"]
  }
}
